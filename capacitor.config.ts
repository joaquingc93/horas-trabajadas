import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.horastrabajadas',
  appName: 'horas-trabajadas',
  webDir: 'dist/horas-trabajadas/browser'
};

export default config;
